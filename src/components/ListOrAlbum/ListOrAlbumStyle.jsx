import styled from 'styled-components';

const TopChatNavbar = styled.header`
  display: flex;
  justify-content: flex-end;
  background-color: var(--white);
  width: 100%;
  border-bottom: 1px solid #dbdbdb;
  padding: 12px 16px;
  gap: 10px;
`;

export default TopChatNavbar;
